# Agent Instructions

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`rishi-harti768/multiplayer-travel-agent`). See `docs/agents/issue-tracker.md`.

### Triage labels

Standard triage label mapping (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo configuration (`CONTEXT.md` and `docs/adr/` at root). See `docs/agents/domain.md`.
